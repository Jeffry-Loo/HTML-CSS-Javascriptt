# 第 5 章：综合项目：任务看板

这个项目把 HTML、CSS、JavaScript 串起来：做一个本地任务看板。

功能：

- 添加任务。
- 标记完成。
- 删除任务。
- 按全部、进行中、已完成筛选。
- 使用 `localStorage` 保存任务。
- 响应式布局，手机和桌面都能用。

完整代码在：

- [examples/task-board/index.html](examples/task-board/index.html)
- [examples/task-board/styles.css](examples/task-board/styles.css)
- [examples/task-board/app.js](examples/task-board/app.js)

## 5.1 项目结构

```text
task-board/
  index.html
  styles.css
  app.js
```

分工：

- `index.html`：页面骨架、表单、列表容器。
- `styles.css`：整体布局、卡片、按钮、响应式。
- `app.js`：任务数据、事件、渲染、本地保存。

## 5.2 HTML 骨架思路

页面需要：

- 一个 `form` 用来添加任务。
- 一个筛选按钮组。
- 一个任务列表区域。
- 一个统计区域。

关键结构：

```html
<form id="taskForm" class="task-form">
  <label for="taskInput">新任务</label>
  <div class="task-form__row">
    <input id="taskInput" name="title" type="text" required>
    <button type="submit">添加</button>
  </div>
</form>

<div class="filters" aria-label="任务筛选">
  <button type="button" data-filter="all">全部</button>
  <button type="button" data-filter="active">进行中</button>
  <button type="button" data-filter="done">已完成</button>
</div>

<ul id="taskList" class="task-list"></ul>
```

注意：

- 表单输入框有 `label`。
- 筛选按钮用 `button`，不是链接。
- 任务列表用 `ul`，每个任务用 `li`。
- `data-filter` 给 JavaScript 读取筛选类型。

## 5.3 CSS 思路

全局先设置稳定基础：

```css
*,
*::before,
*::after {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: Arial, "Microsoft YaHei", sans-serif;
  line-height: 1.5;
}
```

布局用 Grid 和 Flex：

```css
.app-shell {
  width: min(100% - 32px, 920px);
  margin-inline: auto;
}

.task-form__row {
  display: flex;
  gap: 10px;
}

.task-item {
  display: grid;
  grid-template-columns: auto 1fr auto;
  gap: 12px;
  align-items: center;
}
```

小屏幕上让表单按钮换行：

```css
@media (max-width: 560px) {
  .task-form__row {
    flex-direction: column;
  }
}
```

## 5.4 JavaScript 数据设计

任务数组：

```js
let tasks = [
  {
    id: 1716000000000,
    title: "学习 HTML",
    done: false
  }
];
```

每个任务需要：

- `id`：唯一标识，用于删除和切换。
- `title`：任务标题。
- `done`：是否完成。

当前筛选状态：

```js
let currentFilter = "all";
```

## 5.5 渲染函数

渲染是把数据变成 DOM。

```js
function renderTasks() {
  taskList.innerHTML = "";

  const visibleTasks = getVisibleTasks();

  for (const task of visibleTasks) {
    const item = createTaskElement(task);
    taskList.append(item);
  }
}
```

拆函数的好处：

- `renderTasks` 只管整体渲染。
- `getVisibleTasks` 只管筛选。
- `createTaskElement` 只管创建单个任务 DOM。

## 5.6 添加任务

```js
taskForm.addEventListener("submit", (event) => {
  event.preventDefault();

  const title = taskInput.value.trim();
  if (title === "") {
    return;
  }

  tasks.push({
    id: Date.now(),
    title,
    done: false
  });

  saveTasks();
  renderTasks();

  taskForm.reset();
  taskInput.focus();
});
```

流程：

1. 阻止表单刷新页面。
2. 读取输入值。
3. 验证非空。
4. 添加到数组。
5. 保存。
6. 重新渲染。
7. 清空并聚焦输入框。

## 5.7 删除和完成任务

任务按钮是动态生成的，适合用事件委托：

```js
taskList.addEventListener("click", (event) => {
  const action = event.target.dataset.action;
  const item = event.target.closest("[data-id]");

  if (!action || !item) {
    return;
  }

  const id = Number(item.dataset.id);

  if (action === "toggle") {
    toggleTask(id);
  }

  if (action === "delete") {
    deleteTask(id);
  }
});
```

修改数据：

```js
function toggleTask(id) {
  tasks = tasks.map((task) => {
    if (task.id !== id) {
      return task;
    }

    return {
      ...task,
      done: !task.done
    };
  });

  saveTasks();
  renderTasks();
}
```

删除数据：

```js
function deleteTask(id) {
  tasks = tasks.filter((task) => task.id !== id);
  saveTasks();
  renderTasks();
}
```

## 5.8 保存到 localStorage

```js
function saveTasks() {
  localStorage.setItem("html2.tasks", JSON.stringify(tasks));
}

function loadTasks() {
  const saved = localStorage.getItem("html2.tasks");
  return saved ? JSON.parse(saved) : [];
}
```

用 `html2.tasks` 这种带命名空间的 key，可以避免和别的项目冲突。

## 5.9 项目扩展方向

完成基础版后，可以继续加：

- 任务优先级。
- 截止日期。
- 搜索任务。
- 一键清除已完成。
- 拖拽排序。
- 导出 JSON。
- 使用真实后端 API。

扩展时仍然遵守主线：

1. HTML 先增加必要结构。
2. CSS 补上视觉状态。
3. JavaScript 增加数据字段和事件逻辑。

