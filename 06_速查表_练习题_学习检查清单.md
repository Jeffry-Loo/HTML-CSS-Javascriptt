# 第 6 章：速查表、练习题与学习检查清单

## 6.1 HTML 速查

基础结构：

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>页面标题</title>
  </head>
  <body>
    <main></main>
  </body>
</html>
```

常用语义标签：

- `header`：头部。
- `nav`：导航。
- `main`：主要内容。
- `section`：主题区域。
- `article`：独立内容。
- `aside`：补充内容。
- `footer`：底部。

常用内容标签：

- `h1` 到 `h6`：标题。
- `p`：段落。
- `a`：链接。
- `img`：图片。
- `ul` / `ol` / `li`：列表。
- `table` / `tr` / `th` / `td`：表格。
- `form` / `label` / `input` / `button`：表单。

## 6.2 CSS 速查

选择器：

```css
p {}
.class {}
#id {}
.parent .child {}
.parent > .child {}
[type="email"] {}
button:hover {}
input:focus-visible {}
```

盒模型：

```css
*,
*::before,
*::after {
  box-sizing: border-box;
}

.box {
  margin: 16px;
  border: 1px solid #ddd;
  padding: 16px;
  width: 300px;
}
```

Flex：

```css
.row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}
```

Grid：

```css
.grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 16px;
}
```

响应式：

```css
@media (min-width: 768px) {
  .layout {
    grid-template-columns: 240px 1fr;
  }
}
```

## 6.3 JavaScript 速查

变量：

```js
const name = "Jeffry";
let count = 0;
```

函数：

```js
function greet(name) {
  return `Hello, ${name}`;
}

const add = (a, b) => a + b;
```

数组：

```js
items.map((item) => item.name);
items.filter((item) => item.done);
items.find((item) => item.id === id);
items.reduce((total, item) => total + item.price, 0);
```

DOM：

```js
const button = document.querySelector("#saveButton");

button.addEventListener("click", () => {
  console.log("clicked");
});
```

创建元素：

```js
const item = document.createElement("li");
item.textContent = "新任务";
list.append(item);
```

localStorage：

```js
localStorage.setItem("tasks", JSON.stringify(tasks));
const tasks = JSON.parse(localStorage.getItem("tasks") || "[]");
```

Fetch：

```js
async function loadData() {
  const response = await fetch("/api/data");
  if (!response.ok) {
    throw new Error("请求失败");
  }
  return response.json();
}
```

## 6.4 必会概念检查

HTML：

- 能说出 `head` 和 `body` 的区别。
- 能正确使用标题层级。
- 能写有 `label` 的表单。
- 知道 `button` 和 `a` 的使用区别。
- 知道图片为什么要写 `alt`。

CSS：

- 能解释盒模型。
- 知道 `class` 和 `id` 选择器区别。
- 能用 Flex 做横向排列。
- 能用 Grid 做卡片网格。
- 能写至少一个媒体查询。
- 知道 `:hover` 和 `:focus-visible` 的作用。

JavaScript：

- 能解释 `let` 和 `const` 的区别。
- 能使用数组 `map`、`filter`、`find`。
- 能写函数并返回值。
- 能监听按钮点击。
- 能读取输入框的值。
- 能创建 DOM 元素。
- 能用 `localStorage` 保存 JSON 数据。
- 能用 `fetch` 请求 JSON。

## 6.5 综合练习

练习 1：个人主页

要求：

- 使用语义化结构。
- 包含头像、简介、技能列表、联系表单。
- 使用 CSS 做响应式布局。
- 用 JavaScript 实现“切换主题”。

练习 2：课程卡片列表

要求：

- 数据存在 JavaScript 数组中。
- 页面由 JavaScript 渲染。
- 可以按难度筛选。
- 卡片使用 CSS Grid 排列。

练习 3：本地记账小工具

要求：

- 表单输入项目名称、金额、类型。
- JavaScript 计算收入、支出、余额。
- 数据保存到 `localStorage`。
- 删除记录后页面和统计同步更新。

## 6.6 常见问题答案

问题：什么时候用 `class`，什么时候用 `id`？

答案：CSS 样式优先用 `class`。页面唯一元素可以用 `id` 给 JavaScript 定位，比如 `taskForm`。不要在同一页面重复 `id`。

问题：为什么按钮在表单里会自动提交？

答案：`button` 在表单内部默认类型通常是 `submit`。如果只是普通按钮，写 `type="button"`。

问题：为什么我的 CSS 不生效？

答案：检查四件事：CSS 文件路径是否正确、选择器是否匹配、是否被后面的规则覆盖、浏览器是否缓存旧文件。

问题：为什么 JavaScript 找不到元素？

答案：脚本可能执行太早。给 `script` 加 `defer`，或把脚本放在 `body` 末尾。

问题：`innerHTML` 和 `textContent` 怎么选？

答案：插入纯文本用 `textContent`。只有明确需要解析 HTML，且内容可信时才用 `innerHTML`。

## 6.7 学习节奏建议

第 1 周：

- 学 HTML 结构。
- 每天手写一个小页面。
- 重点练表单和语义标签。

第 2 周：

- 学 CSS 盒模型、Flex、Grid。
- 模仿 3 个常见布局：导航、卡片、表单。

第 3 周：

- 学 JavaScript 基础。
- 熟练数组、对象、函数、条件。

第 4 周：

- 学 DOM 和事件。
- 做任务看板、记账工具、搜索筛选列表。

每学完一个知识点，都用同一句话检查自己：

> 我能不能不用看答案，写出一个小例子？

